{
  "name": "cube",
  "version": "1.0.0",
  "description": "cube",
  "license": "ISC",
  "author": "mbook",
  "type": "commonjs",
  "main": "build/index.js",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "verify": "node verify/run.mjs count",
    "format": "npx prettier --write \"**/*.ts\""
  },
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1"
  }
}
