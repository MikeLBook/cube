{
  "name": "cube",
  "version": "1.0.0",
  "description": "cube",
  "license": "ISC",
  "author": "mbook",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/app.ts src/cube3d.ts --bundle --outdir=.",
    "watch": "esbuild src/app.ts src/cube3d.ts --bundle --outdir=. --watch"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
